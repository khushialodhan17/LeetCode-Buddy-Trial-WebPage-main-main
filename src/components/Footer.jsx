import React from "react";

const Footer = () => (
  <footer className="footer bg-section">
    <div className="container">
      <p>&copy; {new Date().getFullYear()} LeetCode Buddy</p>
    </div>
  </footer>
);

export default Footer;
